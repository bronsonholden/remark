<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Remark</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <script>
      if (localStorage.theme === "dark" || (!("theme" in localStorage) && window.matchMedia("(prefers-color-scheme: dark)").matches)) {
        document.documentElement.classList.add("dark")
      } else {
        document.documentElement.classList.remove("dark")
      }
    </script>
    <%= social_meta_tags %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= favicon_link_tag "favicon.svg" %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="text-gray-800 dark:bg-gray-900 dark:text-gray-100">
    <main>
      <div class="sticky z-50 top-0 bg-white dark:bg-gray-900 border-b border-gray-200 dark:border-gray-700">
        <div class="flex sm:max-w-lg mx-auto h-14 items-center px-3 sm:px-0">
          <%= render(BackButtonComponent.new) %>
          <div class="grow"></div>
          <%= link_to feed_path, aria: {label: "Feed"}, class: "leading-[0] hover:bg-gray-100 dark:text-gray-400 dark:hover:text-gray-100 dark:hover:bg-gray-800 p-3 rounded-full" do %>
            <%=
              heroicon "x",
                variant: :outline,
                options: { class: "block h-5 w-5 inline align-text-top" },
                path_options: { stroke_width: 2 }
            %>
          <% end %>
        </div>
      </div>
      <div class="container mx-auto p-6">
        <%= yield %>
      </div>
    </main>
  </body>
</html>
